<?xml version="1.0"?>
<!--
  NOTE: User and query level settings are set up in "users.xml" file.
  If you have accidentally specified user-level settings here, server won't start.
  You can either move the settings to the right place inside "users.xml" file
   or add <skip_check_for_incorrect_settings>1</skip_check_for_incorrect_settings> here.
-->
<yandex>
    <logger>
        <level>debug</level>
        <log>/var/log/byconity-server/dm.log</log>
        <errorlog>/var/log/byconity-server/dm.log</errorlog>
        <size>1000M</size>
        <count>10</count>
    </logger>

    <path>/var/lib/byconity-server/</path>
    <cnch_data_retention_time_in_sec>5</cnch_data_retention_time_in_sec>
    <background_schedule_pool_size>6</background_schedule_pool_size>

    <daemon_manager>
        <port>9920</port>
        <daemon_jobs>
            <job>
                <name>PART_GC</name>
                <interval>10000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>PART_MERGE</name>
                <interval>10000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>CONSUMER</name>
                <interval>10000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>DEDUP_WORKER</name>
                <interval>10000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>PART_CLUSTERING</name>
                <interval>10000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>GLOBAL_GC</name>
                <interval>5000</interval>
                <disable>false</disable>
            </job>
            <job>
                <name>TXN_GC</name>
                <interval>600000</interval>
                <disable>false</disable>
            </job>
            <job>
                <!-- Detects worker restarts and re-warms the TTL disk cache. -->
                <name>WORKER_CACHE_PRELOAD</name>
                <interval>30000</interval>
                <disable>false</disable>
            </job>
        </daemon_jobs>
    </daemon_manager>

    <!-- WORKER_CACHE_PRELOAD tuning (seconds). -->
    <!-- Skip a worker until it has been (re)registered at least this long (avoid preloading a half-initialized worker). -->
    <worker_cache_preload_grace_sec>30</worker_cache_preload_grace_sec>
    <!-- Minimum gap between preload broadcasts; collapses bursts (RM failover, rolling restart, mass scale-out) into one fan-out. -->
    <worker_cache_preload_cooldown_sec>300</worker_cache_preload_cooldown_sec>
    <!-- Restrict warming to this database; empty = every database the server hosts. -->
    <worker_cache_preload_database></worker_cache_preload_database>

    <storage_configuration>
        <disks>
            <default/>
            <server_local_disk1>
                <path>/var/lib/byconity-server/server_local_disk/data/0/</path>
            </server_local_disk1>
            <server_hdfs_disk0>
                <path>/user/clickhouse/</path>
                <type>bytehdfs</type>
            </server_hdfs_disk0>
        </disks>
        <policies>
            <default>
                <volumes>
                    <local>
                        <default>server_local_disk1</default>
                        <disk>default</disk>
                        <disk>server_local_disk1</disk>
                    </local>
                </volumes>
            </default>
            <cnch_default_hdfs>
                <volumes>
                    <hdfs>
                        <default>server_hdfs_disk0</default>
                        <disk>server_hdfs_disk0</disk>
                    </hdfs>
                </volumes>
            </cnch_default_hdfs>
        </policies>
    </storage_configuration>
    <cnch_config>/etc/byconity-server/cnch_config.xml</cnch_config>
</yandex>
